#include <stdio.h>
int main()
{
	int numArr[10] = {0};			// 배열의 요소를 모두 0으로 초기화 
	
	printf("%d\n", numArr[0]);		// 모두 0이므로 전부 0이 출력 
	printf("%d\n", numArr[5]);
	printf("%d\n", numArr[9]);

	return 0;
} 
